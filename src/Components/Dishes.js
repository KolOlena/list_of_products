import React from "react";

const Dishes = ({dishesPoint}) => {
    return (
        <div>
        <div>Select what you want to cook:</div>
            {dishesPoint}
        </div>
    );
}

export default Dishes
